# Parser

## What should be done?

A sub program that take a line from the `interface` module and 
*make sense* of it...



## Which lines?

- `cmd` or `/path/to/cmd` should execute the command or program
at `/path/to/cmd` in a child process.

- `< infile cmd` or `cmd < infile` should execute the command or program at `/path/to/cmd` in a child process and redirect its
stdin to infile.

- ``
- ``

## Making sense
